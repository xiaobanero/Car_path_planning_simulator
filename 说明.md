# 路径规划模拟程序操作说明

## 项目简介
这是一个基于Python和Pygame的交互式路径规划模拟程序，用于模拟小车在路径上的行驶和避让行为。

## 核心功能
1. **路径绘制**：通过鼠标绘制路径点
2. **起点终点设置**：设置小车的起点和终点
3. **小车生成**：在起点生成小车，自动规划路径
4. **拥塞处理**：小车相遇时按序号依次通行
5. **智能等待**：没有对应终点时小车会等待

## 操作说明

### 基本操作
- **左键单击**：在无模式下绘制单个路径点
- **按住左键拖动**：连续生成路径点
- **右键单击**：在起点生成小车（只能在起点生成）
- **中键单击**：删除路径点、起点、终点或小车

### 模式切换
- **无模式**：绘制路径点
- **设置起点**：在路径点上设置起点
- **设置终点**：在路径点上设置终点

### 按钮功能
- **无模式**：切换到路径绘制模式
- **设置起点**：切换到起点设置模式
- **设置终点**：切换到终点设置模式
- **重置所有**：清空所有路径、起点、终点和小车

### 小车行为
1. **路径规划**：小车会自动计算最短路径
2. **拥塞处理**：多辆小车相遇时，按生成序号依次通行
3. **等待机制**：
   - 遇到拥塞时等待1秒后恢复
   - 没有对应终点时会在起点等待
   - 终点出现后自动出发
4. **到达终点**：小车到达终点后会自动消失

## 路径规则
- **路径点**：通过左键单击或拖动生成
- **相邻连接**：路径点会自动与相邻节点连接
- **起点编号**：起点会自动编号（1、2、3...）
- **终点编号**：终点会自动编号（1、2、3...）
- **对应关系**：起点1生成的小车前往终点1，以此类推

## 视觉提示
- **路径点**：白色方块
- **起点**：绿色方块，显示编号
- **终点**：红色方块，显示编号
- **小车**：蓝色圆形，显示ID和目标终点
- **等待状态**：显示"等待"文字
- **等待终点**：显示"等待终点"文字

## 控制台信息
程序会在控制台显示以下信息：
- 生成小车的信息
- 小车状态变化
- 错误提示
- 操作反馈

## 运行方法
1. 确保安装了Python和Pygame
2. 运行 `python main.py`
3. 按照上述操作说明使用程序

## 注意事项
- 只能在起点右键生成小车
- 起点和终点必须设置在路径点上
- 小车会自动寻找对应编号的终点
- 多辆小车会按生成顺序依次通行
